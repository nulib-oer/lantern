# pandoc settings for html output format
# for all available options, see 
# https://pandoc.org/MANUAL.html#default-files

metadata-file: 
  - source/data/metadata.yml

to: plain

template: assets/templates/oai.xml